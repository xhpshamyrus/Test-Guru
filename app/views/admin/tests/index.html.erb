<h1><%= t('.header') %></h1>

<p>
	<%= link_to t('.new'), new_admin_test_path,  class: 'btn btn-primary' %>
</p>

<table class="table table-striped">
	<thread>
		<tr>
			<th><%= t('.title') %></th>
			<th><%= t('.level') %></th>
			<th><%= t('.category') %></th>
			<th><%= t('.question') %></th>
			<th colspan="3"><% t('.actions') %></th>
		</tr>
	</thread>

	<%= render @tests %>
</table>