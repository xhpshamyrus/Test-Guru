<div class="container">
	<h1><%= t('.header') %></h1>

	<table class="table">
		<thead>
			<tr>	
				<th><%= t('.question') %></th>
				<th><%= t('.url') %></th>
				<th><%= t('.user') %></th>
			</tr>
		</thead>

		<%= render @gists %>
	</table>
</div>